age = 15
name = "James"
shoe_size = 8

# Never put underscores at the beginning of code
_happy = True
print(_happy)

student2 = "Stevey"
print(student2)

print(name + " is " + str(age) + " years old and he wears a size " + str(shoe_size) + " shoe.")

# Use + to join things together
# Python has trouble converting strings into integers
# we must turn strings into integers
# To do that, we wrap the integer in str()
# Always add space around the strings when using +
